interface Props {
    title: string
    description: string
}

export default function ExpandableCard({ title = "codap", description = "codap" }: Props) {
    return (
        <div>

        </div>
    );
}